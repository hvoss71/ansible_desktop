---
- hosts: localhost
  connection: local
  become: true
  gather_facts: true

  vars:
    benutzername: zeugnis
    desktop_subpath: "Schreibtisch"  # deutsche Desktop-Bezeichnung
    wrapper_path: "~{{ benutzername }}/.data/wine_reset_wrapper_zenity.sh"
  tasks:
    - name: Home-Verzeichnis des Benutzers ermitteln
      ansible.builtin.shell: "eval echo ~{{ benutzername }}"
      register: user_home
      changed_when: false

    - name: Zenity installieren (falls benötigt)
      apt:
        name: zenity
        state: present
        update_cache: yes
      when: ansible_facts.os_family == "Debian"

    - name: Wrapper-Skript für Reset mit Zenity erstellen
      ansible.builtin.copy:
        dest: "{{ user_home.stdout }}/.data/wine_reset_wrapper_zenity.sh"
        content: |
          #!/bin/bash
          if command -v zenity >/dev/null 2>&1; then
            if zenity --question --text="Lässt sich das Noteneingabe-Programm nicht mehr starten?" --ok-label="Ja" --cancel-label="Nein"; then
              rm -rf "$HOME/.wine"
              zenity --info --text=".wine-Verzeichnis wurde gelöscht."
            else
              zenity --info --text="Löschung abgebrochen."
            fi
          else
            # Fallback: Textprompt (falls Zenity doch nicht verfügbar ist)
            read -p "Lässt sich das Noteneingabe-Programm nicht mehr starten? (Ja/Nein): " user_choice
            case "$user_choice" in
              Ja|ja|YES|Yes|yes)
                rm -rf "$HOME/.wine"
                echo ".wine-Verzeichnis wurde gelöscht."
                ;;
              *)
                echo "Löschung abgebrochen."
                ;;
            esac
          fi
        mode: '0755'
        owner: "{{ benutzername }}"
        group: "{{ benutzername }}"

    - name: Desktop-Verknüpfung erstellen (Zenity-Variante)
      ansible.builtin.copy:
        dest: "{{ user_home.stdout }}/{{ desktop_subpath }}/Programm zurücksetzen.desktop"
        content: |
          [Desktop Entry]
          Version=1.0
          Type=Application
          Name=Programm zurücksetzen
          Comment=Löscht das .wine-Verzeichnis nach Bestätigung
          Exec=bash -lc '"{{ wrapper_path | replace(\"~\", \"$HOME\") }}"'
          Icon=system-run
          Terminal=false
          Categories=Utility;
          StartupNotify=true
        mode: '0755'
        owner: "{{ benutzername }}"
        group: "{{ benutzername }}"

